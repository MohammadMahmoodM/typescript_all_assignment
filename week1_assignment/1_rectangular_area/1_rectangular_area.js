// Exercise 1: Write a program that calculates the area of a rectangle.
function rectangle_area(length, width) {
    var rec_area = length * width;
    console.log("a rectangular of length", length, "and width", width, "has an area =", rec_area);
}
rectangle_area(8, 4);
